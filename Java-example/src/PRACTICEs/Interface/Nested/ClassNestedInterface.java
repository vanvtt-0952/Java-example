package PRACTICEs.Interface.Nested;

public class ClassNestedInterface {
	/*
	 * the classes and interfaces themselves can have only two access specifiers
	 * when declared outside any other class: 1) public 2) default (when no
	 * access specifier is specified)
	 */

	/**
	 * 
	 * This interface declared in a class can either be default, public,
	 * protected not private
	 *
	 */
	interface TestIn {
		void show();
	}
}
